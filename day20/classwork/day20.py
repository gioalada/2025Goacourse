# მომხმარებელს შემოაქვს რიცხვი
number = int(input("შეიყვანეთ რიცხვი: "))

# ვამოწმებთ რიცხვს
if number > 0:
    print("ეს რიცხვი დადებითია")
elif number < 0:
    print("ეს რიცხვი უარყოფითია")
else:
    print("ეს რიცხვი ნულია")

# კომენტარები: განსხვავება if-სა და elif-ს შორის:

# if ნიშნავს "თუ", და იგი დამოუკიდებლად ამოწმებს პირობას.
# elif ნიშნავს "თუ არა და", ანუ გამოიყენება მაშინ, როცა პირველივე if პირობა არ შესრულდა
# და საჭიროა სხვა პირობის შემოწმება.

# მაგალითისთვის, თუ გამოიყენებთ მხოლოდ if-ებს:
if number > 0:
    print("დადებითი")
if number < 0:
    print("უარყოფითი")
else:
    print("ნულია")

# ამ შემთხვევაში, ორივე if დამოუკიდებლად შეამოწმებს პირობას.
# მაგალითად, თუ რიცხვი დადებითია (say 5), პირველი if იმუშავებს და დაიბეჭდება "დადებითი",
# მაგრამ მეორე if არ შესრულდება, თუმცა else მაინც დაუკავშირდება მეორე if-ს
# და არასწორად შეიძლება დაიბეჭდოს "ნულია".

# ამიტომ, როცა გვინდა მხოლოდ ერთი პირობა შესრულდეს — უნდა გამოვიყენოთ if-elif-else სტრუქტურა,
# რადგან elif და else მხოლოდ მაშინ შეამოწმებს, როცა წინა პირობა(s) არ შესრულდა.













# მომხმარებელს შემოაქვს რიცხვი
number = int(input("შეიყვანეთ რიცხვი: "))

# ვამოწმებთ დადებითია თუ არა
if number > 0:
    print("ეს რიცხვი დადებითია")

    # ვამოწმებთ ლუწია თუ კენტი
    if number % 2 == 0:
        print("რიცხვი ლუწია")
    else:
        print("რიცხვი კენტია")

# თუ რიცხვი არ არის დადებითი
else:
    print("ეს რიცხვი უარყოფითია ან ნულია")


    # მომხმარებელს შემოაქვს რიცხვი
number = int(input("შეიყვანეთ რიცხვი: "))

# ვამოწმებთ არის თუ არა დადებითი
if number > 0:
    print("რიცხვი დადებითია")

    # ვამოწმებთ ლუწია თუ კენტი
    if number % 2 == 0:
        print("ეს რიცხვი ლუწია")
    else:
        print("ეს რიცხვი კენტია")

# თუ რიცხვი არ არის დადებითი (ანუ უარყოფითია ან ნულია)
else:
    print("რიცხვი არ არის დადებითი (ანუ უარყოფითია ან ნულია)")



# ბოსტნეულის სია
bostneulebi = ["კარტოფილი", "სტაფილო", "კიტრი", "ტომატი", "ხახვი"]

# დაბეჭდეთ მთელი სია
print("მთელი სია:", bostneulebi)

# ცალ-ცალკე ელემენტების დაბეჭდვა ინდექსების გამოყენებით
print("ცალკეული ელემენტები:")
for i in range(len(bostneulebi)):
    print(bostneulebi[i])
